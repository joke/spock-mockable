plugins {
    id 'java'
    id 'jvm-test-suite'
    id 'groovy'
}

dependencies {
    testImplementation project(':spock-mockable')
    testImplementation platform('org.codehaus.groovy:groovy-bom:3.0.9')
    testImplementation platform('org.spockframework:spock-bom:2.1-groovy-3.0')
    testImplementation 'org.slf4j:slf4j-simple:1.7.36'
    testImplementation 'org.spockframework:spock-core'
}

testing {
    suites {
        test {
            useJUnitJupiter()
        }

        spock13groovy24(JvmTestSuite) {
            sources.groovy.srcDirs = project.sourceSets.test.groovy.srcDirs
            dependencies {
                implementation project
                implementation project(':spock-mockable')
                implementation(project.dependencies.platform('org.codehaus.groovy:groovy-bom:2.4.21'))
                implementation(project.dependencies.platform('org.spockframework:spock-bom:1.3-groovy-2.4'))
                implementation 'org.slf4j:slf4j-simple:1.7.36'
                implementation 'org.spockframework:spock-core'
            }
        }
        spock13groovy25(JvmTestSuite) {
            sources.groovy.srcDirs = project.sourceSets.test.groovy.srcDirs
            dependencies {
                implementation project
                implementation project(':spock-mockable')
                implementation(project.dependencies.platform('org.codehaus.groovy:groovy-bom:2.5.16'))
                implementation(project.dependencies.platform('org.spockframework:spock-bom:1.3-groovy-2.5'))
                implementation 'org.slf4j:slf4j-simple:1.7.36'
                implementation 'org.spockframework:spock-core'
            }
        }
        spock20groovy25(JvmTestSuite) {
            sources.groovy.srcDirs = project.sourceSets.test.groovy.srcDirs
            dependencies {
                implementation project
                implementation project(':spock-mockable')
                implementation(project.dependencies.platform('org.codehaus.groovy:groovy-bom:2.5.16'))
                implementation(project.dependencies.platform('org.spockframework:spock-bom:2.0-groovy-2.5'))
                implementation 'org.slf4j:slf4j-simple:1.7.36'
                implementation 'org.spockframework:spock-core'
            }
        }
        spock20groovy30(JvmTestSuite) {
            sources.groovy.srcDirs = project.sourceSets.test.groovy.srcDirs
            dependencies {
                implementation project
                implementation project(':spock-mockable')
                implementation(project.dependencies.platform('org.codehaus.groovy:groovy-bom:3.0.9'))
                implementation(project.dependencies.platform('org.spockframework:spock-bom:2.0-groovy-3.0'))
                implementation 'org.slf4j:slf4j-simple:1.7.36'
                implementation 'org.spockframework:spock-core'
            }
        }
        spock21groovy25(JvmTestSuite) {
            sources.groovy.srcDirs = project.sourceSets.test.groovy.srcDirs
            dependencies {
                implementation project
                implementation project(':spock-mockable')
                implementation(project.dependencies.platform('org.codehaus.groovy:groovy-bom:2.5.16'))
                implementation(project.dependencies.platform('org.spockframework:spock-bom:2.1-groovy-2.5'))
                implementation 'org.slf4j:slf4j-simple:1.7.36'
                implementation 'org.spockframework:spock-core'
            }
        }
        spock21groovy30(JvmTestSuite) {
            sources.groovy.srcDirs = project.sourceSets.test.groovy.srcDirs
            dependencies {
                implementation project
                implementation project(':spock-mockable')
                implementation(project.dependencies.platform('org.codehaus.groovy:groovy-bom:3.0.9'))
                implementation(project.dependencies.platform('org.spockframework:spock-bom:2.1-groovy-3.0'))
                implementation 'org.slf4j:slf4j-simple:1.7.36'
                implementation 'org.spockframework:spock-core'
            }
        }
        spock22groovy25(JvmTestSuite) {
            sources.groovy.srcDirs = project.sourceSets.test.groovy.srcDirs
            dependencies {
                implementation project
                implementation project(':spock-mockable')
                implementation(project.dependencies.platform('org.codehaus.groovy:groovy-bom:2.5.16'))
                implementation(project.dependencies.platform('org.spockframework:spock-bom:2.2-M1-groovy-2.5'))
                implementation 'org.slf4j:slf4j-simple:1.7.36'
                implementation 'org.spockframework:spock-core'
            }
        }
        spock22groovy30(JvmTestSuite) {
            sources.groovy.srcDirs = project.sourceSets.test.groovy.srcDirs
            dependencies {
                implementation project
                implementation project(':spock-mockable')
                implementation(project.dependencies.platform('org.codehaus.groovy:groovy-bom:3.0.9'))
                implementation(project.dependencies.platform('org.spockframework:spock-bom:2.2-M1-groovy-3.0'))
                implementation 'org.slf4j:slf4j-simple:1.7.36'
                implementation 'org.spockframework:spock-core'
            }
        }
        spock22groovy40(JvmTestSuite) {
            sources.groovy.srcDirs = project.sourceSets.test.groovy.srcDirs
            dependencies {
                implementation project
                implementation project(':spock-mockable')
                implementation(project.dependencies.platform('org.apache.groovy:groovy-bom:4.0.2'))
                implementation(project.dependencies.platform('org.spockframework:spock-bom:2.2-M1-groovy-4.0'))
                implementation 'org.slf4j:slf4j-simple:1.7.36'
                implementation 'org.spockframework:spock-core'
            }
        }
    }
}

test {
    enabled = false
    useJUnitPlatform()
}

tasks.withType(Test) {
    forkEvery 1
}

tasks.named('check') {
    dependsOn(testing.suites)
}
